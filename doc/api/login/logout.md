# logout

``` python
def logout(cookie):
```

该库只有一个logout方法，用于退出登录


#### 调用该方法传入cookie退出登录，成功后cookie将会失效

> logout(cookie)

``` json
{
    "code": 0,
    "status": true,
    "ts": 1663034005,
    "data": {
        "redirectUrl": "https://passport.biligame.com/crossDomain?DedeUserID=&DedeUserID__ckMd5=&SESSDATA=&bili_jct=&gourl=javascript%3Ahistory.go%28-1%29"
    }
}
```
